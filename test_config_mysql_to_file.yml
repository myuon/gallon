in:
  type: sql
  driver: mysql
  table: users
  database_url: root:root@tcp(localhost:3306)/test
  schema:
    id:
      type: string
    name:
      type: string
    age:
      type: number
    created_at:
      type: number
out:
  type: file
  filepath: ./output.jsonl
  format: jsonl
